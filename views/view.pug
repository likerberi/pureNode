doctype html
html
    head
        meta(charset='utf-8')
    body
        h1 
            a(href='/topic') Simple-Server-Side JS
        ul
            each topic in topics
                li
                    a(href='/topic/'+topic)= topic
        article
            h2= id
            = description

        div
            a(href='/topic/new') new