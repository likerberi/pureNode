doctype html
html
    head
        meta(charset='utf-8')
    body
        h1 
            a(href='/topic') Simple-Server-Side JS
        ul
            each topic in topics
                li
                    a(href='/topic/'+topic.id)= topic.title
        article
            if topic
                h2= id
                = description
            else
                h2 Welcome
                | This is SSJS tutorial.
        div
            a(href='/topic/new') new